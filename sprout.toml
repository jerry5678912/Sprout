[project]
name = "sprout"
version = "0.3.0"
main = "examples/fibonacci.sprout"
authors = ["Sprout contributors"]
description = "A programming language platform with tooling, application libraries, reproducible builds, and package distribution."
license = "MIT"

[paths]
source = ["examples"]
modules = ["examples/modules"]

[dependencies]
