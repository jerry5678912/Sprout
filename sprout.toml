[project]
name = "sprout"
version = "0.2.0"
main = "examples/fibonacci.sprout"
authors = []
description = "A programming language platform with tooling, an experimental VM, and practical application libraries."
license = "MIT"

[paths]
source = ["examples"]
modules = ["examples/modules"]

[dependencies]
