language "chinese-pack"

# 猜数字：输入提示后，猜出一到十之间的秘密数字。
定义 开始游戏():
  秘密数字 = 7
  猜测次数 = 0

  输出 "欢迎来到猜数字游戏！"
  输出 "秘密数字在 1 到 10 之间。"

  当 真:
    玩家猜测 = 整数(输入("请输入你的猜测："))
    猜测次数 = 猜测次数 + 1

    如果 玩家猜测 < 秘密数字:
      输出 "太小了，再试一次。"
    否则如果 玩家猜测 > 秘密数字:
      输出 "太大了，再试一次。"
    否则:
      输出 "猜对了！你用了", 猜测次数, "次。"
      跳出

开始游戏()
