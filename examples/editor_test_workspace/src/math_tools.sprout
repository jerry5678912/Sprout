## Add two numbers.
def add(a, b):
  return a + b

## Clamp a value to an inclusive range.
def clamp_value(value, low, high):
  if value < low:
    return low
  if value > high:
    return high
  return value
