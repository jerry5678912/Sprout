[project]
name = "sprout_cavern"
version = "0.1.0"
main = "src/main.sprout"
authors = []
description = "Dogfood 2D game simulation with movement, collision, score, and rendering."
license = "MIT"

[paths]
source = ["src"]
modules = ["modules"]

[dependencies]
